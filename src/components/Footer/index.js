const Footer = () => {
    return (
        <div style={{
            padding: 10,
            backgroundColor: "#0C356A",
        }}>
            <p style={{
                textAlign: "center",
                color: "#fff"
            }}>this is footer section</p>
        </div>
    );
};

export default Footer;